package com.syntax.class23;

public class AreaOfMethods {
	//1. Create 1 class in which create a methods that will calculate the area (volume) of 
	//* Rectangle
	//* Square
	//* Box
	//Use separate class to test your code
	
	public double area(double length, double height) {
		return length *height;
	}
	
	public double area(double length) {
		return length *length;
	}
	
	public double area (double length, double hight, double width) {
		return (length*hight*width);
	}
	

}
